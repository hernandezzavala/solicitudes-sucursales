const Device = require('./devices');
const Subsidiary = require('./subsidaries');
const Task = require('./tasks');
const Type = require('./types');
const User = require('./users');

module.exports = {
    Device,
    Subsidiary,
    Task,
    Type,
    User   
}